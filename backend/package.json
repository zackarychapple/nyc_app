{
  "name": "nyc-demo-backend",
  "version": "2.0.0",
  "description": "Hono API for NYC Demo — Cloudflare Workers + LakeBase",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 3001",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.9.7",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260218.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.40.3"
  }
}
