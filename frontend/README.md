# Frontend (TanStack Start SSR)

## Scripts

```bash
pnpm dev       # SSR dev server on :3000
pnpm build     # Production build
pnpm preview   # Preview build
pnpm deploy    # Build + wrangler deploy
```

## Env

- `VITE_API_URL` — backend API origin (for example `http://localhost:3001`)

## Routing

- `/` registration flow
- `/dashboard` live dashboard

File-based routes live in `src/routes`.
